<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/app_bar_picture"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/conversation_tool_ber_margin"
            app:srcCompat="@mipmap/ic_global_chat"
            android:contentDescription="App bar picture."/>

        <TextView
            android:id="@+id/conversation_user_name"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:textSize="@dimen/conversation_user_name_font_size"
            android:textColor="@color/conversation_user_name_text_color"
            android:gravity="center_vertical"
            tool:text="User Name"/>
    </LinearLayout>

</FrameLayout>